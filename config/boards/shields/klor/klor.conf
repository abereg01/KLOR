# Uncomment these two line to add support for encoders to your firmware
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Uncomment the following line to enable the KLOR OLED Display
CONFIG_ZMK_DISPLAY=y

# Uncomment the following lines to enable per key RGB.
# CONFIG_WS2812_STRIP was removed in the Zephyr 4.1 / ZMK main upgrade —
# the WS2812-SPI driver is now selected automatically by the devicetree
# `compatible = "worldsemi,ws2812-spi"` node in boards/<board>.overlay.
CONFIG_ZMK_RGB_UNDERGLOW=y
